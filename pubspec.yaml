name: axon_mesh
version: 1.0.0

dependencies:
  flutter:
    sdk: flutter
  flutter_p2p_connection: ^1.0.0
  permission_handler: ^10.0.0
  crypto: ^3.0.0

# Additional dependencies for Axon Mesh V4
# Add any other dependencies here

# Flutter application settings
flutter:
  uses-material-design: true
