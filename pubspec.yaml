name: axon_mesh
description: A professional decentralized ghost mesh network.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_p2p_connection: ^2.0.2
  permission_handler: ^11.3.1
  path_provider: ^2.1.3
  sqflite: ^2.3.3

flutter:
  uses-material-design: true
